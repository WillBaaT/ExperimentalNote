<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"> 
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
    <title>CHYLab - Experimental Note</title>
    <meta name="Will" content="CHYLab - Experimental Note" />
    <script type="text/javascript" src="index.js"></script>
    <link href="/assets/application.css" media="screen" rel="stylesheet" type="text/css" />
    <script src="/assets/application.js?body=1" type="text/javascript"></script>
  </head>
<body>
  <div class="container">
  <header class="clearfix">
        <h1 border="10px" class="clearfix">Post</h1>
  </header>
</div>
<div class="create">
  <div class="container" style="text-align:center">
    <%= form_for(@message) do |f| %>
      <div class="field">  
        <%= f.text_area :content, :class => "post_text", :placeholder => 'Think before submitting' %><%= @message.errors.full_messages_for(:title).first %>
        <% if @message.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@message.errors.count, "error") %> prohibited this person from being saved:</h2>
        <% end %>
      </div>
      <div class="actions"> 
        <%= f.submit "Submit", :class => "post_button" %>
        <!--<%= link_to "Cancle", "/",:class => "post_button"%>
        -->
      </div> 
    <%end%>
    <style>
    .post_button{
      height: 60px;
      width: 40%;
    }
    .post_text{
      margin: 10px;
      height: 150px;
      width: 80%;
    }
    </style>
  </div>
</div>
</body>
</html>